import { Text, View } from "react-native";

function Controls() {
  return (
      <View>
        <Text>Higher or lower?</Text>
        {/* + / - buttons */}
      </View>
  );
}

export default Controls;
